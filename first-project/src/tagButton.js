
export default function TagButton({ children }) {
    return (
        <>
            
            <button className="tagButton">
                Click me
                {children}
                </button>
        </>
    )
}